{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint After Task Completion",
      "trigger": "PostTaskExec",
      "description": "Lint all code after any task completion to catch code quality issues",
      "action": {
        "type": "command",
        "command": "npx eslint --max-warnings 0 --no-error-on-unmatched-pattern . 2>&1 | tail -30"
      }
    }
  ]
}