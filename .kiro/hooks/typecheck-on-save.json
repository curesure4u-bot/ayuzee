{
  "version": "v1",
  "hooks": [
    {
      "name": "TypeScript Check on Save",
      "trigger": "PostFileSave",
      "description": "TypeScript type-check on every file save to catch type errors immediately",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx tsc --noEmit 2>&1 | head -50",
        "timeout": 90
      }
    }
  ]
}